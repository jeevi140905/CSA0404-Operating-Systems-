#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main()
{
    pid_t pid;

    // Create a new process
    pid = fork();

    if (pid == 0)
    {
        // Child Process
        printf("Child Process:\n");
        printf("PID  = %d\n", getpid());
        printf("PPID = %d\n", getppid());
    }
    else if (pid > 0)
    {
        // Parent Process
        printf("Parent Process:\n");
        printf("PID  = %d\n", getpid());
        printf("PPID = %d\n", getppid());
    }
    else
    {
        // Fork failed
        printf("Fork failed\n");
    }

    return 0;
}
Output
Status :Successfully executed
Time:
0.0000 secs
Memory:
1.652 Mb
Your Output
Parent Process: PID = 16374, Child PID = 16375
Child Process: PID = 16375, Parent PID = 1
